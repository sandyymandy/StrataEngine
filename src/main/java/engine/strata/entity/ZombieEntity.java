package engine.strata.entity;

import engine.strata.world.World;

public class ZombieEntity extends Entity{
    public ZombieEntity(EntityKey<?> key, World world) {
        super(key, world);
    }
}
